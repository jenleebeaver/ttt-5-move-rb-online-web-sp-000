#!/usr/bin/env ruby
#note that this file doesn't need a .rb

require_relative '../lib/move.rb'

# Code your CLI Here
board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]
puts "Welcome tp Tic Tac Toe!"

puts "Where would you like to go?"
input = gets.strip
index = input_to_index(input)
move(board, index, current_player="X")
display_board(move)
